[project]
name = "aijudge-analytics"
version = "0.0.1"
description = "Agreement metrics and PoC gate evaluation (S9)"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.8",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/aijudge_analytics"]
