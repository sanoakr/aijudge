# ストレージと WAL アーカイブの健全性確認。**遷移時だけメールする。**
[Unit]
Description=aiJudge storage and WAL archive health check
# 失敗したらメールで知らせる（#422）。
OnFailure=aijudge-notify@%n.service
After=postgresql.service

[Service]
Type=oneshot
User=root
ExecStart=/usr/local/sbin/aijudge-storage-check.sh
Nice=10
